trigger ShowError on Contact (before update) {
   /*for(Contact con: trigger.new){
        if((trigger.oldmap.get(con.Id).Email !='' || trigger.oldmap.get(con.Id).Email != null)&&
           (trigger.oldmap.get(con.Id).Phone != null))
        {
        	if(con.Email != trigger.oldmap.get(con.Id).Email 
               || con.Phone != trigger.oldmap.get(con.Id).Phone ){
          		con.addError('Hello');  
        }
        }
        
       
    }*/
}